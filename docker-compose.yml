services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8501:8501"
    env_file:
      - .env
    environment:
      DATASET_CACHE_DIR: /app/data/cache
      PORT: "8501"
    volumes:
      - ./data/cache:/app/data/cache
