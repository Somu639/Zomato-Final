# Leave empty when using Next.js rewrites to the API (default in dev)
# Set in production if the browser calls the API directly
NEXT_PUBLIC_API_BASE_URL=

# Server-side rewrite target (Next.js dev/build and Docker)
API_URL=http://127.0.0.1:8000
