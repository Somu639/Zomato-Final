# Local dev: leave empty to use Next.js rewrites to http://127.0.0.1:8000 (see next.config.ts)

# Vercel production: your Railway API URL (no trailing slash)
NEXT_PUBLIC_API_BASE_URL=https://your-service.up.railway.app
